# Public Render API used by Launchy frontend (production / Vercel build).
VITE_API_ORIGIN=https://lauchy.onrender.com

# Provide Logo.dev keys via deployment environment variables (for example, Vercel/Render),
# and do not commit them to version control.